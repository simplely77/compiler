// 基本除法运算测试
a = 10;
b = 2;
c = a / b;
print c;  // 应该输出 5

// 测试整除
d = 20;
e = -4;
f = d / e;
print f;  // 应该输出 5

// 测试不能整除的情况
g = -7;
h = 2;
i = g / h;
print i;  // 应该输出 3（整数除法会截断）

// 测试除数为0的情况
m = 10;
n = 0;
o = m / n;  // 应该显示错误消息 "Error: Division by zero!"
print o;    // 这行不会执行，因为程序会在上一行退出 